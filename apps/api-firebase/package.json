{
  "name": "api-firebase",
  "private": true,
  "main": "dist/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "node build.mjs",
    "build:watch": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:firebase-admin --external:firebase-functions --watch",
    "serve": "pnpm run build && concurrently \"pnpm run build:watch\" \"firebase emulators:start\"",
    "serve:functions": "pnpm run build && firebase emulators:start --only functions",
    "shell": "pnpm run build && firebase functions:shell",
    "start": "pnpm run shell",
    "sync-secrets": "node sync-secrets.mjs",
    "deploy": "pnpm run build && pnpm run sync-secrets && firebase deploy --only functions",    
    "logs": "firebase functions:log",
    "emulator": "firebase emulators:start"
  },
  "dependencies": {
    "@self-stats/contracts": "workspace:*",
    "@self-stats/progression-system": "workspace:*",
    "@self-stats/soul-topology": "workspace:*",
    "@google/genai": "^1.41.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "overrides": {
    "p-retry": "4.6.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "esbuild": "^0.27.3",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.32.0",
    "firebase-functions-test": "^3.4.1",
    "typescript": "^5.7.3"
  }
}
