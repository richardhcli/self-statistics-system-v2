{
  "name": "self-statistics-system-v2",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "npm run build:watch --workspace=api-firebase"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1"
  }
}
