{
  "name": "self-statistics-system-v2",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "dev:web": "pnpm --filter web run dev",
    "dev:api": "pnpm --filter api-firebase run serve",
    "dev": "concurrently \"pnpm run dev:web\" \"pnpm run dev:api\"",

    "build:web": "pnpm --filter web run build",
    "build:api": "pnpm --filter api-firebase run build",
    "build": "concurrently \"pnpm run build:web\" \"pnpm run build:api\"",
    
    "deploy": "pnpm run build && firebase deploy"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "concurrently": "^9.2.1"
  }
}
